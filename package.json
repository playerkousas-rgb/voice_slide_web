{
  "name": "voice-cue-slide-tool-static",
  "version": "0.2.4",
  "private": true,
  "description": "No-install browser-based voice cue slide player MVP for GitHub + Vercel",
  "scripts": {
    "dev": "python -m http.server 3000",
    "build": "rm -rf public && mkdir -p public && cp index.html styles.css app.js sample_cues_50th.csv sample_project.vcue.json README.md DEPLOYMENT.md PRODUCT_PLAN.md GOOGLE_DRIVE_PHASE2.md VERCEL_GITHUB_DEPLOYMENT.md vercel.json public/",
    "start": "python -m http.server 3000"
  }
}
